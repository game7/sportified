<h1>Listing teams</h1>

<table border="1" cellpadding="5" style="border-collapse: collapse;">
  <tr>
    <th>Name</th>
    <th>Short Name</th>
    <th>Related</th>
    <th>Actions</th>
  </tr>

<% @teams.each do |team| %>
  <tr>
    <td><%= team.name %></td>
    <td><%= team.short_name %></td>
    <td><%= link_to 'Roster', season_team_players_path(@season, team) %></td>
    <td>
      <%= link_to 'Show', [@season, team] %>
      <%= link_to 'Edit', edit_season_team_path(@season, team) %>
      <%= link_to 'Destroy', [@season, team], :confirm => 'Are you sure?', :method => :delete %>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Team', new_season_team_path(@season) %>
