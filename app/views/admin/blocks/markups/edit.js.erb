$('#dialog-modal').html("<%= escape_javascript( render(:partial => "admin/blocks/markups/form" ) )%>");
$('#dialog-modal').dialog({
  title: 'Edit Markup',
  height: 500,
  width: 800,
  modal: true,
  buttons: {
    Save: function() {
      $(this).children("form").submit();     
    },
    Cancel: function() {
      $(this).dialog("close");
    }
  }
}); 
