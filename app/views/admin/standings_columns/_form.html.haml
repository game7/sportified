= semantic_form_for( :standings_column, :url => admin_standings_layout_standings_columns_path(layout), :remote => true ) do |f|
  = f.inputs do
    = f.input :field_name, :as => :select, :collection => TeamRecord.list_fields
    = f.input :title
    = f.input :description
  = f.buttons do
    = f.submit
