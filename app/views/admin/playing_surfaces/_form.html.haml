= simple_form_for [:admin, @location, @playing_surface] do |f|
  .panel.panel-default
    .panel-body
      = f.input :name
      = f.button :wrapped, :cancel => return_url

:css

  .playing_surface_name {
    width: 400px;
  }