= simple_form_for [:admin, @season] do |f|
  = f.input :name
  = f.input :starts_on
  = f.association :leagues, :collection => League.all.asc(:name), :as => :check_boxes
  = f.button :wrapped, :cancel => return_url



