$('body').append("<%= escape_javascript( render(:partial => @partial ) )%>");
var $modal = $('.modal.edit_hockey_statsheet');
$modal.find(':submit').click(function() {
  console.log('hi')
  $modal.find('form').submit();
})
$modal.modal({
  onHidden: function() {
    $(this).remove();
  }
}).modal('show');
