$('#dialog-modal').html("<%= escape_javascript( render(:partial => "admin/hockey_goaltenders/new" ) )%>");
$('#dialog-modal').dialog({
  title: 'Add Goaltender',
  height: 420,
  width: 500,
  modal: true,
  buttons: {
    Save: function() {
      $(this).children("form").submit();
    },
    Cancel: function() {
      $(this).dialog("close");
    }
  }
});
