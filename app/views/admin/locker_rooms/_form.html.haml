= simple_form_for [:admin, @location, @locker_room] do |f|
  .panel.panel-default
    .panel-body
      = f.input :name
      = f.input :playing_surface_id, :collection => @playing_surfaces
      = f.input :preference, :collection => @preferences
      = f.button :wrapped, :cancel => return_url
