$('body').append("<%= escape_javascript( render(:partial => "edit", :layout => false ) )%>");
var $modal = $('.modal.edit_page');
$modal.find(':submit').click(function() {
  $modal.find('form').submit();
})
$modal.modal({
  onHidden: function() {
    $(this).remove();
  }
}).modal('show');
