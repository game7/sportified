var $modal = $('#dialog-modal');
<% @modal_title = "Edit Page Settings" %>
$modal.html("<%= escape_javascript( render(:partial => "edit", :layout => "layouts/modal" ) )%>");
$modal.modal();
$modal.find('.submit').click(function(){
  $modal.find('form').submit();
});
