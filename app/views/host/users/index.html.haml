- title 'Users'
%table.ui.celled.table
  %thead
    %tr
      %th First
      %th Last
      %th Email
      %th Actions
  %tbody
    - @users.each do |user|
      %tr
        %td= user.first_name
        %td= user.last_name
        %td= user.email
        %td= link_to 'Show', [:host, user], class: [:ui, :tiny, :primary, :button]
