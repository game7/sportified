- title @item.title
- page_actions do
  = link_to 'Edit', edit_item_path(@item), class: [:ui, :primary, :button] if current_user_is_admin?

!= MarkdownService.html @item.description

%table.ui.table
  %thead
    %th OPTION
    %th.center.aligned PRICE
    %th
  %tbody
    - @item.variants.each do |variant|
      %tr
        %td
          .title{style: 'font-weight: bold'}= variant.title
          .description= variant.description
        %td.center.aligned= variant.price ? number_to_currency(variant.price) : 'FREE'
        - if @item.active
          %td.collapsing= link_to 'Register', new_variant_registration_path(variant), class: [:ui, :mini, :primary, :button]

%hr
= link_to 'Back', items_path
