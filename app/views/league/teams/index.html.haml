- title "Teams"

.controls
  .left
    Season: #{team_season_select(@division)}
.clear

= render :partial => "team_cards"

= render :partial => "quick_add"

= link_to 'New Team', new_league_division_team_path(@division)
