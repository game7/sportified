- title 'Standings - ' + @season.name

%div
  Season: 
  = collection_select("season", :id, @division.seasons, :slug, :name)

= render :partial => "standings_table", :locals => { :team_records => @team_records, :columns => @columns, :edit => false }

