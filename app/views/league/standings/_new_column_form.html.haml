= semantic_form_for( :standings_column, :url => league_division_standings_columns_path(division), :remote => true ) do |f|
  = f.inputs do
    = f.input :field_name, :as => :select, :collection => TeamRecord.list_fields
    = f.input :title
    = f.input :description
  = f.buttons do
    = f.submit
