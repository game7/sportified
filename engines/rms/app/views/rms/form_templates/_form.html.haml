= form_for @form_template do |f|
  - if @form_template.errors.any?
    #error_explanation
      %h2= "#{pluralize(@form_template.errors.count, "error")} prohibited this form_template from being saved:"
      %ul
        - @form_template.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
