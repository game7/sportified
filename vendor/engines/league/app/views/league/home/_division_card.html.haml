.division-card
  %strong #{division.name} Division
  %ul
    %li #{link_to 'Home', league_division_path(division.slug)}
    %li #{link_to 'Schedule', league_schedule_path(division.slug)}
    %li #{link_to 'Scoreboard', league_scoreboard_path(division.slug)}
    %li #{link_to 'Standings', league_standings_path(division.slug)}
    %li #{link_to 'Teams', league_teams_path(division.slug)}


