.division-card
  %strong #{link_to division.name, league_division_path(division.slug)}
  .action.block
    .division_edit #{link_to 'Edit', edit_admin_division_path(division)}
    .division_delete #{link_to 'Delete', [:admin, division], :confirm => 'Are you sure?', :method => :delete}

