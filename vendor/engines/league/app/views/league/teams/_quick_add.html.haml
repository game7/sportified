= semantic_form_for [:league, @division, Team.new], :remote => true do |f|
  = f.inputs do
    = f.input :name
    = f.input :season_id, :as => :hidden, :value => @season.id
  = f.buttons do
    = f.commit_button

