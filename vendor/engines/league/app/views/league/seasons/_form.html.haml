= semantic_form_for [:league, @season] do |f|
  = f.inputs do
    = f.input :name
    = f.input :starts_on
  = f.buttons do
    = f.commit_button
    - unless @season.new_record?
      %li #{link_to 'Cancel', league_season_friendly_path(@season.slug)}
    - else
      %li #{link_to 'Cancel', league_path}

