= link_to 'All', admin_teams_path(:division_id => params[:division_id])
- @seasons.each do |season|
  = " | "
  = link_to season.name, admin_teams_path(:season_id => season.id, :division_id => params[:division_id])


