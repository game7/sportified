= link_to 'All', admin_games_path(:division_id => params[:division_id])
- @seasons.each do |season|
  = " | "
  = link_to season.name, admin_games_path(:season_id => season.id, :division_id => params[:division_id])


