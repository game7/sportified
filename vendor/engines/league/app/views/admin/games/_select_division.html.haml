= link_to 'All', admin_games_path(:season_id => params[:season_id])
- @divisions.each do |division|
  = " | "
  = link_to division.name, admin_games_path(:division_id => division.id, :season_id => params[:season_id])

