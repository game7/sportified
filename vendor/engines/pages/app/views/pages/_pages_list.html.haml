
//%ul
//  - pages.each do |page|
//    %li 
//      = link_to page.title, page_path(page.path)
//      = render :partial => 'pages_list', :locals => { :pages => page.children }
- pages.each do |page|
  - page.level.times do
    = "-"
  = link_to page.title, page_path(page.path)
  (#{link_to "Edit", edit_page_path(page.id)} | #{link_to "Design", design_page_path(page.id)})
  %br
