= semantic_form_for [@page] do |f|
  = f.inputs do
    = f.input :title
    = f.input :meta_keywords
    = f.input :meta_description
  = f.buttons do
    = f.commit_button
    %li #{link_to 'Cancel', admin_seasons_path}
