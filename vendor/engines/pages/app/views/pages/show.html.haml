- title @page.title

- content_for :sidebar do
  %ul
    %li #{link_to 'Edit This Page', edit_page_path(@page)}

#blocks
  - @page.blocks.each do |b| 
    = render :partial => "blocks/show", :locals => { :block => b }
