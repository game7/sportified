<div style="display: flex; height: 100%; justify-content: center; margin-top: 40px;">
  <div class="btn-group" role="group" aria-label="...">
    <button type="button" class="btn btn-default btn-lg {{if awayTeamSelected "active"}}" {{action "selectAwayTeam"}}>{{awayTeam.name}}</button>
    <button type="button" class="btn btn-default btn-lg {{if homeTeamSelected "active"}}" {{action "selectHomeTeam"}}>{{homeTeam.name}}</button>
  </div>
</div>
