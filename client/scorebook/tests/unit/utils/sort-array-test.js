import sortArray from '../../../utils/sort-array';
import { module, test } from 'qunit';

module('Unit | Utility | sort array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortArray();
  assert.ok(result);
});
